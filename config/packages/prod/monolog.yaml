monolog:
  handlers:
    app:
      type: stream
      path: "php://stderr"
      level: info

    security_debug:
      type: stream
      path: "php://stderr"
      level: debug
      channels: ["security"]
