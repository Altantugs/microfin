{% for label, messages in app.flashes %}
  {% for m in messages %}<div class="flash {{ label }}">{{ m }}</div>{% endfor %}
{% endfor %}

<h1>Нууц үг мартсан уу?</h1>
<p>Имэйлээ оруулж илгээнэ үү. Хэрэв бүртгэлтэй бол сэргээх линк очно.</p>

<form method="post" action="{{ path('forgot_password') }}">
  <label for="fp-email">Имэйл</label>
  <input id="fp-email" name="email" type="email" placeholder="Имэйл" aria-label="Имэйл" autocomplete="email" required>
  <button type="submit">Илгээх</button>
</form>

<p style="margin-top:1rem;">
  <a href="{{ path('app_login') }}">Нэвтрэх рүү буцах</a> · <a href="{{ path('public_home') }}">Нүүр</a>
</p>
