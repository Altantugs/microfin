<h1>Нууц үг мартсан уу?</h1>

{% for label, messages in app.flashes %}
  {% for msg in messages %}
    <div class="flash {{ label }}">{{ msg }}</div>
  {% endfor %}
{% endfor %}

<form method="post" action="{{ path('forgot_password') }}">
  <label for="email">Имэйл</label>
  <input id="email" name="email" type="email" required autocomplete="email" placeholder="example@domain.com" aria-label="Имэйл" />
  <button type="submit">Сэргээх линк авах</button>
</form>

<p style="margin-top:1rem;">
  <a href="{{ path('app_login') }}">Нэвтрэх рүү буцах</a> ·
  <a href="{{ path('public_home') }}">Нүүр</a>
</p>
