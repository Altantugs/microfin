{% extends 'base.html.twig' %}
{% block title %}Excel Upload{% endblock %}

{% block body %}
  <h1>📊 Excel upload</h1>

  {% for label, messages in app.flashes %}
    {% for msg in messages %}
      <div class="flash-{{ label }}">{{ msg }}</div>
    {% endfor %}
  {% endfor %}

  <form method="post" enctype="multipart/form-data" style="border:1px solid #ddd;padding:12px;">
    <div style="margin-bottom:12px">
      <input type="file" name="excel" accept=".xlsx,.xls,.csv" required>
    </div>

    <label style="display:block;margin:8px 0;">
      <input type="checkbox" name="clear" value="1">
      Өмнөх бичлэгүүдийг устгаад шинээр импортлох
    </label>

    <button type="submit">Upload</button>
  </form>

  <p style="margin-top:12px">
    <a href="https://microfin-ui52.onrender.com">Нүүр</a> ·
    <a href="{{ path('report_summary') }}">Тайлан</a>
  </p>
{% endblock %}
